# 测试与验证

[返回首页](README.md) · [架构](docs/ARCHITECTURE.md) · [截图更新](docs/SCREENSHOTS.md#更新截图)

版本：1.3（5）。验证日期：2026-09-20。

## 环境与结果

- Xcode 26.2 / Swift 6.2.3。
- iPhone 17 Pro 模拟器，iOS 26.3.1。
- iPhone Release 签名构建成功，已完成真机覆盖安装。
- 最新一次完整测试运行 **43 项通过、0 失败**：38 项核心、网络、缓存与收益率测试，5 项 UI 流程。
- 验证了收益百分比的初始资金口径、缺失 / 零 / 非有限分母处理、负收益、单位切换、拖动选点、范围切换、隐私隐藏和重启后的单位恢复。
- 月历覆盖完整月份、首周星期列对齐和月末日期；历史清理仍仅在模拟网络与演示预览中验证。

## 自动化覆盖

| 模块 | 重点 |
| --- | --- |
| 数据与 API | URL 规范化、接口白名单、动态 K 线列、分页去重、完整历史检查、共享令牌续期、有限 GET 重试 |
| 统计口径 | UTC 日期边界、按平仓日归属、完整月份与闰年、持平交易分母、缺失数值处理 |
| 收益百分比 | 按初始资金计算、区间累计与亏损、无效基准拒绝、单位切换与选点保留、重启恢复 |
| 缓存与响应 | 常用范围预计算、缓存立即命中、快速切换结果顺序、异步磁盘恢复、服务器隔离、历史变化失效 |
| 历史清理 | 排除未平仓及截止日记录、删除前核对身份和状态、拒绝未结束委托、失败停止、禁止不确定 DELETE 重试、重新同步、禁止重复执行同一预览 |
| 界面 | 四页导航、连接表单校验、共享时间范围、默认拖动读数、浅深色、日历切月、历史清理预览 |

历史删除通过 URLProtocol 模拟服务器验证；UI 仅预览演示数据。验证过程没有删除真实交易记录。真实服务的登录、数据读取与恢复另行人工验证；账号、地址、缓存及真实截图不纳入仓库。

## 本地运行

```bash
xcodebuild -project FreqLens.xcodeproj -scheme FreqLens \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test
```

可在 Xcode 中选择本机可用的 iOS 26+ iPhone 模拟器。保持默认模拟器签名，以便钥匙串测试正常运行。

## 性能与边界

- 模拟器中 100 次已缓存区间的状态切换约 12–18 ms；这是统计状态测试，不代表真机端到端画面耗时。
- 切换日期不发起网络请求；未命中缓存时后台计算，并保留带明确区间标注的上一份结果。
- 尚未进行长期运行或真实弱网压力测试。后台不持续轮询。
- 真机安装需要有效 Apple 签名；仓库不包含证书或设备描述文件。

## 记录与复现

以上是 1.3（5）的本机验证记录，仓库尚未配置自动 CI。修改代码后应按实际影响运行相关测试，不能把本记录当作后续提交的自动通过结果。

需要保留测试包时，可以为 `xcodebuild test` 添加 `-resultBundlePath DerivedData/Regression.xcresult`，路径必须尚不存在。使用 Xcode 打开结果包，或运行：

```bash
xcrun xcresulttool get test-results summary \
  --path DerivedData/Regression.xcresult --compact
```

UI 测试与截图均使用演示数据。文档或图片更新检查 Markdown 链接、图片可读性、内容准确性及敏感数据；不需要为这类变更重复运行完整 App 测试。
